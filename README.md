//TODO
        // Add curves to acceleration/deacceleration - LOW - transitioned to low, as I added lerps and they are ok
        // Add random movement at high points - LOW
        // Refactor inputs (drop events, lol) - MID
        // Refactor state machine (events, same shit) - MID
        // Add debug functions (checkpoint tp, debug lines) - MID
        // Fix bug quick snapping while holding input and releasing hand - HIGH

        // concept corpse moving mechanics
        //Fix double jump bug
